version: 2

deploy:
  steps:
    setupVariables:
      after:
        - echo "hello world"
        - name: My Custom Step
          run: |
            echo "$MY_STRING"
          env:
            MY_STRING: "Hello V2 Custom Flow"
    terraformApply:
      after:
        - name: Ansible Demo
          run: |
            echo "Run Ansible Playbook"
            
