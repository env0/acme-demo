version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Install Checkov
          run: pip3 install checkov
    terraformPlan:
      after:
        - name: Checkov Scan
          run: checkov -f $ENV0_TF_PLAN_JSON --quiet
