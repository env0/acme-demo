environments:
  vpc:
    name: "VPC"
    templateName: "Random String"
    revision: kmac-workflow-easy
  eks:
    name: "EKS"
    templateName: "ACME S3 Bucket"
    revision: kmac-workflow-easy
    needs: 
      - vpc
  db:
    name: "DB"
    templateName: "Pulumi s3"
    needs:
      - vpc
  service:
    name: "Service"
    templateName: "kmac-simple-remote-backend"
    needs:
      - db
      - eks
  # rps-app:
  #   name: "rps app"
  #   templateName: "k8s - rps app"
  #   needs:
  #     - eks
      