environments:
  vpc:
    name: "VPC"
    templateName: "Random String"
    revision: kmac-workflow-easy
  eks:
    name: "EKS"
    templateName: "ACME S3 Bucket"
    revision: kmac-workflow-easy
    needs: 
      - vpc
  db:
    name: "DB"
    templateName: "Pulumi s3"
    needs:
      -vpc
  # rps-app:
  #   name: "rps app"
  #   templateName: "k8s - rps app"
  #   needs:
  #     - eks
      