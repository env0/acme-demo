version: 1

deploy:
  steps:
    setupVariables:
      after:
        - aws cloudformation deploy --stack-name test-cf-ec2 --template-file ec2.yaml
destroy:
  steps:
    setupVariables:
      after:
        - aws cloudformation delete-stack --stack-name test-cf-ec2
