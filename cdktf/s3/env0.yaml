version: 1

deploy:
  steps:
    setupVariables: &cf
      after:
        - sudo npm install -g cdktf-cli@latest
        - npm install
        - cdktf list
        - cdktf synth -o template # make sure your env0 CF template points to "cf.template"  

destroy:
  steps:
    setupVariables: *cf