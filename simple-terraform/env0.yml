version: 1

deploy:
  steps:
    terraformInit:
      before:
        - ssh -V
        - sudo apk add --update --no-cache openvpn
        - openvpn --version
    terraformPlan:
      before:
        - ls -la
