version: 2

deploy:
  steps:
    setupVariables:
      after:
        - echo "hello world"
        - name: My Custom Step
          run: |
            echo "$MY_STRING"
          env:
            MY_STRING: "Hello V2 Custom Flow"
    # terraformInit:
    #   before:
    # terraformPlan:
    #   after:
    #     - terraform show -json .tf-plan > tf-plan.json
    #     - echo "OPA Check" 1>&2
#  onCompletion:
#    - type: string
#  onSuccess:
#    - type: string
#  onFailure:
#    - type: string