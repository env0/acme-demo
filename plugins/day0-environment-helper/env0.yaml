version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Scan for Folder Changes
          run: |
            echo "Scanning... $ROOT_FOLDER"
            ls -la $ROOT_FOLDER
          env:
            ROOT_FOLDER: "dynamic-environments"
