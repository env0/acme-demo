version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Scan for Folder Changes
          run: |
            echo "Scanning... $($TF_VAR_root_folder)"
            ls -la $TF_VAR_root_folder
            echo "test"
            ls -la $ENV0_ROOT_DIR/dynamic-environments
          env:
            TF_VAR_root_folder: "$ENV0_ROOT_DIR/dynamic-environments"
