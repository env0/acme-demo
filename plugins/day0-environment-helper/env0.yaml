version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: Scan for Folder Changes
          run: |
            ./day0helper.sh
            # echo "Scanning... $($TF_VAR_root_folder)"
            # ls -la $ENV0_ROOT_DIR/$TF_VAR_root_folder
          env: []
            # TF_VAR_root_folder: "dynamic-environments"
