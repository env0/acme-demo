version: 1

deploy:
  steps:
    setupVariables:
      after:
        - echo "hello world from qa-backend"
    # terraformInit:
    #   before:
    terraformPlan:
      after:
        - terraform show -json .tf-plan > tf-plan.json
        - jq -C tf-plan.json
#  onCompletion:
#    - type: string
#  onSuccess:
#    - type: string
#  onFailure:
#    - type: string