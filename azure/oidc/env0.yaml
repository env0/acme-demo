version: 2

deploy:
  steps: &OIDC
    setupVariables:
      after:
        - name: SETUP OIDC TOKEN 
          run: echo "ARM_OIDC_TOKEN=$ENV0_OIDC_TOKEN" >> $ENV0_ENV
destroy:
  steps: *OIDC  # make sure we also set the token for destroys.
