version: 1

deploy:
  steps:
    setupVariables:
      after:
        - sudo npm install -g aws-cdk
        - cdk ls
        - cdk synth

destroy:
  steps:
    setupVariables:
      after:
        - # destroy CDK