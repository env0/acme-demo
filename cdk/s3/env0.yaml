version: 1

deploy:
  steps:
    setupVariables: &cf
      after:
        - npm install aws-cdk
        - npm install
        - npx aws-cdk ls
        - npx aws-cdk synth > cf.template  # make sure your env0 CF template points to "cf.template"  

destroy:
  steps:
    setupVariables: *cf
