version: 1

deploy:
  steps:
    setupVariables:
      after:
        - npm install -g aws-cdk
        - cdk init app --language javascript
        - cdk ls
        - cdk synth

destroy:
  steps:
    setupVariables:
      after:
        - # destroy CDK